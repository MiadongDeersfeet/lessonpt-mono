<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yunki.lessonpt.curriculum.mapper.ContentDetailMapper">

    <resultMap id="contentDetailResultMap" type="com.yunki.lessonpt.curriculum.domain.ContentDetail">
        <id column="CONTENT_DETAIL_ID" property="contentDetailId"/>
        <result column="CATEGORY_ID" property="categoryId"/>
        <result column="NAME" property="name"/>
        <result column="DISPLAY_ORDER" property="displayOrder"/>
        <result column="MEMO" property="memo" jdbcType="CLOB"/>
        <result column="TARGET_BPM" property="targetBpm"/>
        <result column="EVALUATION_MEMO" property="evaluationMemo" jdbcType="CLOB"/>
        <result column="SHEET_URL" property="sheetUrl"/>
        <result column="YOUTUBE_URL" property="youtubeUrl"/>
        <result column="AUDIO_URL" property="audioUrl"/>
        <result column="CREATED_AT" property="createdAt"/>
        <result column="UPDATED_AT" property="updatedAt"/>
        <result column="STATUS" property="status"
                typeHandler="com.yunki.lessonpt.common.mybatis.typehandler.RecordStatusTypeHandler"/>
        <result column="DELETED_AT" property="deletedAt"/>
    </resultMap>

    <sql id="contentDetailColumns">
        CONTENT_DETAIL_ID, CATEGORY_ID, NAME, DISPLAY_ORDER,
        MEMO, TARGET_BPM, EVALUATION_MEMO, SHEET_URL, YOUTUBE_URL, AUDIO_URL,
        CREATED_AT, UPDATED_AT, STATUS, DELETED_AT
    </sql>

    <sql id="activeContentDetail">
        STATUS = 'Y' AND DELETED_AT IS NULL
    </sql>

    <select id="selectContentDetailById" resultMap="contentDetailResultMap">
        SELECT <include refid="contentDetailColumns"/>
        FROM TB_CONTENT_DETAIL
        WHERE CONTENT_DETAIL_ID = #{contentDetailId}
    </select>

    <select id="selectActiveContentDetailById" resultMap="contentDetailResultMap">
        SELECT <include refid="contentDetailColumns"/>
        FROM TB_CONTENT_DETAIL
        WHERE CONTENT_DETAIL_ID = #{contentDetailId}
          AND <include refid="activeContentDetail"/>
    </select>

    <select id="selectActiveContentDetailByIdAndCategoryId" resultMap="contentDetailResultMap">
        SELECT <include refid="contentDetailColumns"/>
        FROM TB_CONTENT_DETAIL
        WHERE CONTENT_DETAIL_ID = #{contentDetailId}
          AND CATEGORY_ID = #{categoryId}
          AND <include refid="activeContentDetail"/>
    </select>

    <select id="selectActiveContentDetailsByCategoryId" resultMap="contentDetailResultMap">
        SELECT <include refid="contentDetailColumns"/>
        FROM TB_CONTENT_DETAIL
        WHERE CATEGORY_ID = #{categoryId}
          AND <include refid="activeContentDetail"/>
        ORDER BY DISPLAY_ORDER ASC, CONTENT_DETAIL_ID ASC
    </select>

    <insert id="insertContentDetail">
        <selectKey keyProperty="contentDetailId" resultType="long" order="BEFORE">
            SELECT SEQ_CONTENT_DETAIL.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO TB_CONTENT_DETAIL (
            CONTENT_DETAIL_ID, CATEGORY_ID, NAME, DISPLAY_ORDER,
            MEMO, TARGET_BPM, EVALUATION_MEMO, SHEET_URL, YOUTUBE_URL, AUDIO_URL,
            STATUS, CREATED_AT, UPDATED_AT
        ) VALUES (
            #{contentDetailId},
            #{categoryId},
            #{name},
            #{displayOrder},
            #{memo,jdbcType=CLOB},
            #{targetBpm,jdbcType=NUMERIC},
            #{evaluationMemo,jdbcType=CLOB},
            #{sheetUrl,jdbcType=VARCHAR},
            #{youtubeUrl,jdbcType=VARCHAR},
            #{audioUrl,jdbcType=VARCHAR},
            #{status,jdbcType=CHAR,typeHandler=com.yunki.lessonpt.common.mybatis.typehandler.RecordStatusTypeHandler},
            SYSTIMESTAMP,
            SYSTIMESTAMP
        )
    </insert>

    <update id="updateContentDetail">
        UPDATE TB_CONTENT_DETAIL
        SET NAME = #{name},
            MEMO = #{memo,jdbcType=CLOB},
            TARGET_BPM = #{targetBpm,jdbcType=NUMERIC},
            EVALUATION_MEMO = #{evaluationMemo,jdbcType=CLOB},
            SHEET_URL = #{sheetUrl,jdbcType=VARCHAR},
            YOUTUBE_URL = #{youtubeUrl,jdbcType=VARCHAR},
            AUDIO_URL = #{audioUrl,jdbcType=VARCHAR},
            UPDATED_AT = SYSTIMESTAMP
        WHERE CONTENT_DETAIL_ID = #{contentDetailId}
          AND CATEGORY_ID = #{categoryId}
          AND STATUS = 'Y'
          AND DELETED_AT IS NULL
    </update>

    <update id="softDeleteActiveContentDetailsByCategoryId">
        UPDATE TB_CONTENT_DETAIL
        SET STATUS = 'N',
            DELETED_AT = SYSTIMESTAMP,
            UPDATED_AT = SYSTIMESTAMP
        WHERE CATEGORY_ID = #{categoryId}
          AND STATUS = 'Y'
          AND DELETED_AT IS NULL
    </update>

    <update id="softDeleteContentDetail">
        UPDATE TB_CONTENT_DETAIL
        SET STATUS = 'N',
            DELETED_AT = SYSTIMESTAMP,
            UPDATED_AT = SYSTIMESTAMP
        WHERE CONTENT_DETAIL_ID = #{contentDetailId}
          AND CATEGORY_ID = #{categoryId}
          AND STATUS = 'Y'
          AND DELETED_AT IS NULL
    </update>

    <update id="restoreContentDetail">
        UPDATE TB_CONTENT_DETAIL
        SET STATUS = 'Y',
            DELETED_AT = NULL,
            DISPLAY_ORDER = #{displayOrder},
            UPDATED_AT = SYSTIMESTAMP
        WHERE CONTENT_DETAIL_ID = #{contentDetailId}
          AND CATEGORY_ID = #{categoryId}
          AND STATUS = 'N'
    </update>

    <select id="lockContentDetailById" resultMap="contentDetailResultMap">
        SELECT <include refid="contentDetailColumns"/>
        FROM TB_CONTENT_DETAIL
        WHERE CONTENT_DETAIL_ID = #{contentDetailId}
        FOR UPDATE WAIT 3
    </select>

    <select id="selectMaxDisplayOrderByCategoryId" resultType="java.lang.Integer">
        SELECT MAX(DISPLAY_ORDER)
        FROM TB_CONTENT_DETAIL
        WHERE CATEGORY_ID = #{categoryId}
          AND STATUS = 'Y'
          AND DELETED_AT IS NULL
    </select>

    <update id="shiftActiveDisplayOrdersDown">
        UPDATE TB_CONTENT_DETAIL
        SET DISPLAY_ORDER = DISPLAY_ORDER - 1,
            UPDATED_AT = SYSTIMESTAMP
        WHERE CATEGORY_ID = #{categoryId}
          AND STATUS = 'Y'
          AND DELETED_AT IS NULL
          AND DISPLAY_ORDER &gt; #{displayOrder}
    </update>
</mapper>
